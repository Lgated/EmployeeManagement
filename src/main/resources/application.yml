spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/empmgmt
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  servlet:
    multipart:
      max-file-size: 10MB  # 单个文件最大10MB
      max-request-size: 50MB  # 总请求最大50MB
      enabled: true
  redis:
    host: 127.0.0.1
    port: 6379
    # password
    lettuce:
      pool:
        max-active: 16 # 最大连接数
        max-idle: 8 # 最大空闲连接数
        min-idle: 0 # 最小空闲连接数
      timeout: 2000 # 连接超时时间 ms
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: /     # 虚拟主机
    # 开启 publisher confirm 和 return，支持可靠投递
    # 开启「生产者确认机制」 -- correlated 类型说明:是 Spring AMQP 2.1+ 推荐的类型，支持「异步回调」：生产者发送消息后无需阻塞等待，通过 ConfirmCallback 回调函数接收确认结果；
    publisher-confirm-type: correlated
    publisher-returns: true # 开启「生产者回退机制」

server:
  port: 8080

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace

# JWT双Token配置
jwt:
  secret: replace-with-256-bit-secret-key-xxxx  # 生产环境应使用更安全的密钥
  expiration: 3600000  # Token 过期时间(毫秒),默认1小时（兼容旧配置，可选）
  access-ttl-ms: 1800000  # Access Token有效期30分钟
  refresh-ttl-ms: 2592000000  # Refresh Token有效期30天

# 文件存储路径配置
file:
  upload-path: D:/uploads/employee

# 应用配置
# 上线需要改为 true
app:
  https:
    enabled: false  # 开发环境false，生产环境true（影响Cookie的Secure设置）









